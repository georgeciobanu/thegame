<%= form_for(@email_update) do |f| %>
  <% if @email_update.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@email_update.errors.count, "error") %> prohibited this email_update from being saved:</h2>

      <ul>
      <% @email_update.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :subject %><br />
    <%= f.text_field :subject %>
  </div>
  <div class="field">
    <%= f.label :message %><br />
    <%= f.text_area :message %>
  </div>
  <div class="field">
    <%= f.label :date_sent %><br />
    <%= f.date_select :date_sent %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
